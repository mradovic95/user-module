package com.comex.usermodule.endpoint;

import org.springframework.boot.SpringBootConfiguration;
import org.springframework.boot.autoconfigure.EnableAutoConfiguration;

/**
 * Test configuration for endpoint module tests.
 * Provides minimal Spring Boot configuration for WebMvcTest.
 */
@SpringBootConfiguration
@EnableAutoConfiguration
public class EndpointTestConfiguration {
	// Minimal configuration for endpoint tests
}
